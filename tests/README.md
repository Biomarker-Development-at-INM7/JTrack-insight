# Validation Tests

This folder should hold comparison tests between the R reference app and the
Python port.

Examples:

- metadata indexing parity
- QC duplicate detection parity
- subject-level feature parity by sensor
- group summary parity

The R app should remain the scientific reference until these validations are in
place.
